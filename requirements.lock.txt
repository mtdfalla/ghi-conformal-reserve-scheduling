# Exact environment. Generated by code/r1/r1_s10_build_release.py --freeze.
#
# These are the versions in which the cached model predictions were last
# regenerated and checked against their recorded values, on Python 3.11.15 (Linux x86_64).
# code/r1/r1_restore_env.sh installs from this file and fails if it cannot.
#
# requirements.txt is kept for readers who want the dependency list only. Where the
# two disagree, this file is what was actually run.

numpy==2.4.4
pandas==3.0.2
scipy==1.17.1
scikit-learn==1.8.0
statsmodels==0.14.6
pvlib==0.15.2
pyarrow==25.0.1
matplotlib==3.10.9

# Optional: the compact GRU / GRU-TCN baselines only. Their outputs are shipped, and
# they were produced on the version below; every other number in the article comes
# from the CPU stack above.
# torch==2.12.1+cpu   # Python 3.11.9
